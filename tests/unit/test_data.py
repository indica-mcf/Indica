"""Tests of methods on the custom accessors used with xarray objects."""

# DataArray tests:
# ----------------

# Test remapped data has appropriate metadata and provenance

# Test remapped data on correct coordinates

# Check ignored data matches up with NaN values in array

# Check reconstruction of array with ignored data matches data stored values

# Checking getting and setting of equilibrium is consistent and updates PROV
# accordingly.

# Test that checking datatype works for compatible ones

# Test that checking datatype failes for incompatible ones


# Dataset tests:
# --------------

# Check the attach method fails when different specific datatypes are used

# Check attach method fails with incompatible dimensions

# Check attach method updates PROV appropriately

# Check datatype matches contents of Dataset

# Test that checking datatype works for compatible ones

# Test that checking datatype failes for incompatible ones

# Check aggregate function fails for incompatible input

# Check aggregate function combines all compatible input and produces correct
# datatype/PROV
