# .readthedocs.yml

version: 2

sphinx:
  configuration: doc/conf.py
  fail_on_warning: true

python:
  version: 3.8
  install:
    - path: .
      method: pip
      extra_requirements:
        - docs
  system_packages: true
